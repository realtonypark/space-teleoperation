# Fact-check of docs/REPORT.md (draft as of 2026-09-13, HEAD dd26840)

Scope: every non-placeholder claim and number in REPORT.md, checked against the cited research report, SYNTHESIS.md, selection.md, TESTBED_SPEC.md and the code in `spaceteleop/` at HEAD (post audit-fix commit 5dc0be5). Section 9/10 skeletons and `{{RESULTS:...}}` placeholders were ignored. Line numbers refer to REPORT.md at HEAD.

Verdicts: **OK** = matches the source; **WRONG** = contradicts the source or the code; **UNSUPPORTED** = no source found for the figure; **STALE** = was true before commit 5dc0be5 and is no longer true of the code or matrix.

## 1. Claim-by-claim table

| REPORT.md | Claim | Source checked | Verdict | Correction |
|---|---|---|---|---|
| L9 | Question, objective (min latency), acceptance metric | PROGRAM.md Context | OK | — |
| L11 | Conditional yes; Starlink mini-laser first third-party flights Q1 2027; 21 % duty four-site polar; no flown interior arm, no in-orbit demos; sub-100 ms only on direct passes; continuous sub-150 ms one product away | SYNTHESIS §1; space_teleop_prior_art §1–3; leo_link_options §2 b1 | OK | — |
| L15 | Primary 2× Starlink mini-laser, 48 ms median, 15 s spikes to ≈125 ms; fallback four-site polar; safety IDRS/Iridium; GEO disqualified | SYNTHESIS §2 table | OK | — |
| L16 | 50 Hz UDP, 40 B, sent twice; two 720p30 streams per operator in 10.4 Mbps for four operators | multi_operator_bandwidth §2, §3 | OK | Architecture claim only. Note (a) 10.4 Mbps assumes Scenario A at 1 Mbps/stream, which conflicts with L104 and L204 (1.5–2.5 Mbps each); (b) the testbed sends each setpoint once (`ground/loop.py` one `sendto` per tick), duplicate-send is not implemented. |
| L17 | Decision (c): 30 ms playout, hold 300 ms, retract 10 s, ramp resume, envelopes, cutoffs, supervisor | SYNTHESIS §5; `strategies/base.py` (0.030, 0.300, 10.0, vmax 2.0) | OK | — |
| L18 | Leader-follower/VR no force feedback; broker; 50 Hz follower | multi_operator_bandwidth §4; teleop_fundamentals §1.4 | OK | — |
| L19 | Aries 100 kg, 100–300 L 1-atm cylinder, four Dynamixel arms, Orin NX, supervisor, $6–12 M | SYNTHESIS §4; hardware_landscape §7 | OK | $6–12 M is `[derived, unverified]` in hardware_landscape §7; carry `[unverified]` per PROGRAM rule 1. |
| L27 | "Unsafe-motion events across all episodes on all four named profiles" | `metrics/__init__.py` SAFE; `experiments/matrix.py` block S | STALE | The gate is three link-caused counts (`move_in_hold`, `vel_over`, `keepout`); `cage` is reported separately (audit T07). The dropout test that PROGRAM.md's "zero unsafe motion on dropout" needs is block S (`leo_relay_drop1`, `leo_relay_drop12`), not the four named profiles (audit T02: P(outage in a 20 s episode) = 0.9 %). |
| L47 | ETS-VII 5–7 s GEO, all task classes; Orbital Express scripted gates; Xiyuan-0 no numbers; all interior arms on crewed stations; 20–30 ms / 4–20 min vs 0.8–1.1 s regimes | space_teleop_prior_art §1, §2 | OK | — |
| L51 | ETS-VII row | prior_art §1 ETS-VII | OK | — |
| L52 | ROKVISS 20–30 ms, 0.1 % loss, ±2 ms jitter, 245 kbit/s up / 4 Mbit/s down, ≤7.5 min passes | prior_art §1 ROKVISS, Kontur-2 rows | OK | ±2 ms jitter is stated in the Kontur-2 row (same S-band link), not the ROKVISS row; acceptable, cite both rows. |
| L53 | Kontur-2 row incl. Artigas quote | prior_art §1 Kontur-2 | OK | — |
| L54 | Haptics-2/Interact/SUPVIS-Justin/Analog-1 0.8–1.1 s; tail and dropouts hurt | prior_art §1 (820–850, 0.8–1, 832 mean/1132 max, 770–850 ms) | OK | — |
| L55 | Canadarm2/Dextre 0.3–0.5 s secondary; 170 days/yr; >100,000 commands | prior_art §1 [S16][S17] | OK | — |
| L56–58 | Astrobee, GITAI S1, Xiyuan-0 rows | prior_art §1 | OK | — |
| L68–72 | Five-path ranking table incl. Kepler live 24 Aug 2026, TDRS closed Nov 2024, floor ≥474 ms | SYNTHESIS §2 table; leo_link_options §1, §2 | OK | — |
| L74 | hardware_landscape vs leo_link_options disagreement on Kepler | SYNTHESIS §2 para 2; hardware_landscape §6; leo_link_options §2 b2 | OK | — |
| L78 | Floor 4–30 ms; 40 ms median over 19.2 M tests; 15 s cycle; ≈1 % loss; 1.7 outages/h; 25 Gbps; Q1 2027; 48 ms / 90 ms | leo_link_options §1, §2 b1; network_emulation §1.1; SYNTHESIS §2 | OK | "About 1 % loss" comes from leo_link_options §1 (Huston), not network_emulation §1.1 (which gives 0.23–4 % by country); fix the citation. |
| L80 | ×1.45 / ×2.04; 200–300 ms; <200 ms ideal; 163 ms machine loop; two unknowns | teleop_fundamentals §1.2, §4; SYNTHESIS §1, §8 | OK | — |
| L92 (diagram) | "50 Hz UDP setpoints, 40 B, sent twice" | multi_operator_bandwidth Implication | OK | See L16 note: testbed sends once. |
| L95 (diagram) | tau_h 0.17 | `run.py --tau-h` default 0.17 | OK | — |
| L104 (diagram), L204 | Video streams "720p30, 1.5–2.5 Mbps each" | hardware_landscape §4 | WRONG (internal) | Contradicts L16 and L148 ("about 1 Mbps each", 10.4 Mbps total). At 2 × 2.5 Mbps × 4 operators the downlink is 20 Mbps (26 Mbps with FEC). Pick one figure or label the 1.5–2.5 Mbps as the logged-camera rate and 1 Mbps as the live-stream rate. |
| L107 (diagram) | "LeRobot v2 layout" | `record/__init__.py` docstring, `codebase_version` "v2.1" | STALE | Say "LeRobot v2.1 layout" (audit T11 fix added the v2.1 `info.json` keys and `meta/tasks.jsonl`). |
| L115 | Four sites 33.4 passes/day, 9.2 min, 35 min median gap; IDRS 250 kbps, 0.5–1.5 s | network_emulation §1.4; hardware_landscape §6 | OK | — |
| L119 | 40 B payload, 68 B, 27 kbps; 133 B WebRTC; ≤0.2 Mbps at 100 Hz twice; RFC 8854; duplicate-send not NACK | multi_operator_bandwidth §2, Implication | OK | — |
| L121 | Buffer keyed on sequence; stale dropped; "uplink jitter reorders a double-digit share of consecutive packets" | `sat/controller.py` docstring; audit_testbed T17 | WRONG | Measured inversion rate is 5.9 % unloaded and 8.4 % seq-stale in the recorded `leo_relay` cell (T17). Write "about 6–8 %". |
| L121 | Interpolator renders the buffer as it stood 30 ms ago in the satellite's own clock | `strategies/baseline.py` `_playout(buf, now - interp_s)` | OK | — |
| L123 | Telemetry fields; RTT measured at ground with dwell subtracted; 45 B up / 123 B down | `proto/__init__.py` (`<4sIq7fB` = 45 B; `<4sIqIqq21fBH` = 123 B); `ground/loop.py` | OK / STALE | Sizes and dwell subtraction correct. Missing the post-fix behaviour: telemetry is stamped on arrival inside `select()`, not at the next 50 Hz tick (audit T04); the pre-fix `zero` cell read 10 ms RTT instead of ≈1 ms. |
| L129–134 | Six allowed items | SYNTHESIS §5 items 1–6 (verbatim) | OK | Item 5 names acceleration clamps; the testbed implements only the position clamp and the velocity ramp (`baseline.py`). State that the acceleration clamp is architecture, not testbed. |
| L136 | Exclusions; 49 → 65–81 %; (c) over (b); Jetson SEFI | SYNTHESIS §5; teleop_fundamentals §2; hardware_landscape §5 | OK | — |
| L140 | "force coupling is what became unstable in every relay-path program" | space_teleop_prior_art §1 | WRONG | Overstated. Haptics-2 and Interact ran bilateral force feedback over GEO relay and worked (sub-mm insertion at 0.8 s); ETS-VII bilateral was stable at 6–7 s. Only Analog-1 reports force coupling impaired by spikes and dropouts. Say "force coupling is what the delay tail impaired (Analog-1) and what needs passivity control; irrelevant without force feedback". |
| L140 | 5 Hz teleop cost 62 % more time (teleop_fundamentals §1.4; data_pipeline §2.1) | data_pipeline §2.1 ACT row | OK | The 62 % is only in data_pipeline §2.1 (and SYNTHESIS §10); teleop_fundamentals §1.4 says only that 50 Hz outperforms lower rates. Cite data_pipeline alone. |
| L142 | Broker = Astrobee/POIC pattern | multi_operator_bandwidth §4 | OK | — |
| L144 | Synthetic operator: sees delayed telemetry, reacts to a frame tau_h old, bounded Cartesian speed; constant across latencies; 0.17 s = 143–170 ms | `ground/operators.py`; `ground/loop.py`; teleop_fundamentals §3 | OK | — |
| L148 | Global-shutter MIPI/GMSL, intra-refresh, no B-frames, 0–10 ms playout hint, 60–144 Hz; USB webcam disqualifying; two live streams ≈1 Mbps | multi_operator_bandwidth §1.2, §1.3, Implication | OK | — |
| L150–157 | Bandwidth table (8/10.4, 30/39, 16/20.8, 60/78 Mbps; 0.85/1.7 Mbps up); X-band fit; S-band one lean operator | multi_operator_bandwidth §3 | OK | — |
| L161–177 | Latency budget table, 333 / 163 ms; 51 / 14 / 9 %; Anvari 14 of 135; 315 / 885 ms | SYNTHESIS §8 | OK | — |
| L181 | `.npz` per episode, `meta/info.json`, `meta/episodes.jsonl`, extras `cmd_seq`, `rtt_ms`, `owd_up_ms`, `safety_hold` | `record/__init__.py` | STALE | Extras now also include `assist`; the writer also emits `meta/tasks.jsonl`, `task_index`, and the v2.1 `info.json` keys (`total_tasks`, `total_videos`, `total_chunks`, `chunks_size`, `splits`, `data_path`, `video_path`). |
| L181 | LeRobot `timestamp` synthetic; loader rejects jittery stamps | data_pipeline §1.2 | OK | — |
| L185 | Sidecar columns; ≈85 ms obs-action skew on `leo_relay`; smoothness on sidecar (selection.md §3) | `record/__init__.py` docstring (85 ms); `record.write_sat` (t_ns, t_applied_ns, cmd_seq, hold, setpoint, assist, taut); `metrics.applied_speed` | OK | The 85 ms figure is in the recorder docstring, not selection.md; cite `spaceteleop/record/__init__.py`. |
| L186 | Every row carries measured RTT and a `link_state` | data_pipeline §2.3, §3.1; `record/__init__.py` COLS | WRONG | The recorder has no `link_state` column ({0 ok, 1 hold, 2 retract, 3 outage} is data_pipeline's proposal). Rows carry `rtt_ms`, `owd_up_ms` (= RTT/2, an assumption, audit T18) and the boolean `safety_hold`. |
| L187 | robomimic 92 % / 39 vs 66 %; SAL top half 39 → 55 %; SAL, LDLJ, stall fraction reported | data_pipeline §2.1; SYNTHESIS §6 (SAL, LDLJ), §10 (stall fraction); `metrics` | OK | — |
| L193 | 100–190 W; 2–5× a 16U; 55–100 % of 175 W; Aries 100 kg, 2024, $3.5–9.5 M; "≈$1.05 M launch" | hardware_landscape §1 | WRONG (minor, inherited from SYNTHESIS §4) | $1.05 M is hardware_landscape's derived price for a 150 kg ESPA sat ($350 k + 100 kg × $7,000). For the 100 kg configuration the same rule gives ≈$0.7 M. Either say 150 kg or ≈$0.7 M. |
| L195 | 100–300 L, 3.5–10 kg; brushed motors need atmosphere; "vacuum-rated arms such as the 50 kg GITAI IN2 exceed the payload budget" | hardware_landscape §2, §7 | WRONG (overstated) | Source says the IN2 exceeds "the whole payload budget of a 16U" and is "not fundable at this scale"; 50 kg fits Aries' 100 kg but consumes half of it for one arm. Qualify. |
| L197 | Four arms baseline / eight scale; Astrobee 80 % current cutoff, thermostat per motor, ISS since 2019 | SYNTHESIS §4; hardware_landscape §3 | OK | — |
| L199–209 | Subsystem table | SYNTHESIS §4; hardware_landscape §1–7; leo_link_options §2 b1, d | OK | — |
| L211 | $6–12 M, bus dominates | hardware_landscape §7 | OK | Mark `[unverified]` (source does). |
| L213–217 | Three risks incl. <10 % orbit per station, 9.7–25 krad, no continuous-transmit qualification | hardware_landscape "Top three risks" | OK | — |
| L219 | 16U: 40–70 W terminals; ≈8 L cylinder, 1–2 arms; 21 % duty, 9 min / 35 min | SYNTHESIS §4; hardware_landscape §1, §2; network_emulation §1.4 | OK | — |
| L225 | No 6-DoF analog > ≈24 s; air bearing 3 DoF; 0.041 ± 0.005 g; 1e-4 to 1e-5 g for 5–9 s | unique_data_study §1 | OK | — |
| L229–234 | Ranked residual gaps A–D, 5–8 | unique_data_study §2 | OK | — |
| L238–242 | Task table with latency classes | SYNTHESIS §3; teleop_fundamentals §4 | OK | — |
| L244 | Cage, cm/s; two granular + one liquid cell; tasks 2, 5 proxies; capture lower bound; slowness largest hider | unique_data_study §4, §5; SYNTHESIS §3, §10 | OK | — |
| L252 | "Python 3.12 with mujoco, numpy and sgp4 only" | `pyproject.toml`; `sim/__init__.py` imports `robot_descriptions` | WRONG | Runtime dependencies are mujoco, numpy, sgp4 **and robot-descriptions** (SO-100 MJCF); pytest for tests. TESTBED_SPEC.md §sim also names `robot_descriptions`. |
| L254 | Ground: frame tau_h old, 7-slot setpoints at 50 Hz, RTT from echo | `ground/loop.py` | OK | Add arrival stamping (see L123). |
| L255 | Link: two directions, receive thread heaps a release time, drain thread; timer error < 0.3 ms | `link/emulator.py` docstring; network_emulation §2 | OK / STALE | Correct, but omits the post-fix random per-episode phase of the 15 s structure and the outage schedule (audit T03; `Link.struct_phase`, `Link.phase`). Pre-fix the spike was phase-locked to link start and hit every episode's first command. |
| L256 | Sat: seq-keyed buffer, ≈1 kHz strategy call, sim stepped to wall clock, 30 Hz telemetry, keep-out/cage counters, applied log | `sat/controller.py` | OK | — |
| L257 | `capture`: 16 mm box, 2–4.5 cm/s, 0.3–1 rad/s, **20 mm capture envelope**, 50 mm target held 0.2 s | `sim/__init__.py` (BOX 0.008, DRIFT, TUMBLE, GRASP_TOL **0.025**, JAW_GRASP 0.062, TARGET_R 0.05, HOLD_S 0.2) | STALE | Envelope is 25 mm, and capture requires the **physical** jaw closed past 0.062 rad (pad gap = box diagonal), not the close command (audit T01). Measured zero-latency baseline: 21/30 (old predicate) → 16/30 at 20 mm → 18/30 at ≥24 mm. |
| L257 | `peg`: 60 mm, 6 mm clearance, ≤6 mm offset, jam on fixture contact; `capture_chain` tether + alternating targets; cage 0.64 × 0.66 × 0.48 m | `sim/__init__.py` (PEG_H, CLEAR, OFFSET, CAGE_LO/HI) | OK | — |
| L258 | Strategy hook `observe`, `ground_step`, `sat_step`; `--strategy` | `strategies/base.py`; `run.py` | OK | — |
| L264 | Video as `--frame-bytes` ≤ 8 kB at 30 Hz ≈ 2 Mb/s | `sat/controller.py` MAX_FRAME 8192 | OK | — |
| L265 | Grasp kinematic "once inside the 20 mm envelope" | `sim/__init__.py` | STALE | 25 mm and physical jaw, as above. |
| L266 | Realtime clock | `run.py`, `sat/controller.py` docstrings | OK | — |
| L267 | Operator: tau_h 0.17, 0.07 m/s, 5 / 2 cm/s floor, 1.5 mrad noise, jaw at 18 mm | `ground/operators.py` (speed 0.07, SLOW 0.05/0.02, noise 0.0015, close_at 0.018) | OK | — |
| L268 | `--arms N`; one process per cell (GIL) | `run.py`; selection.md §3 | OK | — |
| L269 | Link: "four profiles plus a sweep" | `link/profiles.py` PROFILES | STALE | Six named profiles: `zero`, `direct_gs`, `leo_relay`, `leo_relay_drop1`, `leo_relay_drop12`, `geo_relay`, plus `sweep:<rtt>`. |
| L273–284 | Profile table (delays, jitter, 15 s structure, GE parameters, outages, caps, windows, reordering) | `link/profiles.py` vs SYNTHESIS §7 | OK | Every value matches: GE(0.0034) → 0.50 %, GE(0.001) → 0.15 %, GE(0.0091) → 1.31 %; caps 32 000 / 12 500 000 / 625 000 / 6 250 000 / 2 500 000 / 11 250 000 B/s = the stated bit rates; passes 540 s on / 2100 s off; GEO 2700 s / 45 s; spike 74 up / 37 down over 140 ms; end bump 20 ms / 75 ms. |
| L286 | `sweep:<rtt>` at the 30:18 split; "`sweep:0` separates structure from base delay" | `link/profiles.py` `sweep()` docstring (audit T08) | STALE (incomplete) | Add: `sweep:0` clips the log-normal at 0 so the realised base RTT is ≈12 ms (6.5 / 5.4 ms one way), SD 10.5 / 8.3 instead of 14 / 11; read the x-axis at 0 as "relay structure, ≈12 ms realised". |
| L286 | Four corrections in SYNTHESIS §7 | SYNTHESIS §7 (i)–(iv) | OK | — |
| §7.3 (absent) | Forced-outage profiles | `link/profiles.py` `leo_relay_drop1/drop12`; `matrix.py` block S | STALE | Missing entirely: `leo_relay` plus one forced blackout in both directions starting 6.0 s after link start, 1.0 s (trips hold, returns) or 12.0 s (crosses the 10 s retract, arm stows, ramp-limited resume); `drop12` cells run at 30 s. |
| L290 | Safety: "Four counts": `move_in_hold`, `vel_over`, `keepout`, `cage` | `metrics/__init__.py` SAFE; `aggregate.py` LINK_SAFE (audit T07) | STALE | Gate is the three link-caused counts. `cage` is its own column and excluded because it fires at zero latency (12/30 baseline episodes: the box spawns 10–16 cm from the −y wall and the chase runs the arm into it). Also add `stalls` / `max_dt_s` (CPU-contention flags, audit T06) to the diagnostics list. |
| L290 | Diagnostics `hold`, `retract`, `ramp_clip`, `pos_clamp`, `jams`, `stale` | `metrics` docstring | OK | — |
| L294 | Acceptance definition; 69 % / 49 %; expected to hold for coarse, fail for insertion; per pass on `direct_gs`; smoothness not gated | SYNTHESIS §6; teleop_fundamentals §1.2 | OK | Per-pass reporting exists (`aggregate.py` PASS_MIN 9.2, audit T10). |
| L296 | `demos_per_hour` = 3600 / mean successful wall; gross charges every episode and reset | `metrics.aggregate`; data_pipeline §3.2 | STALE (incomplete) | Wall time now ends at the satellite's success/done stamp; the 1.0 s linger, thread join and npz write are excluded (audit T09/F2, `run.py` `dur`). Say so, since pre-fix numbers carried ≈1 s per episode. |
| L304 | Seven abstentions H03, H04, H05, H06, H13, H17, H18; H13 measured < 5 % penalty | selection.md §1 (7 NO-HYPOTHESIS rows); H13.md | OK | — |
| L308 | Five verifiers, five 1–5 columns (evidence, testability, expected gain, cost 5 = cheap, independence), out of 25; geometry reproduced twice with `orbit.py` | scores_V1.md header and bullet 1 (4-polar and 45-site runs) | OK | — |
| L310 | Baseline near ceiling on `leo_relay` `capture`; discriminating cells | selection.md opening | OK | Post-fix results.md shows the "ceiling" is itself 0.60–0.67 (see missing topic 3). |
| L316 | H10 Twin mechanism, 48 + 30 + 17 ms, reject clauses; "fails capture at the 20 mm envelope" | `strategies/twin.py`; selection.md §2, §3 | OK / STALE | 20 mm → 25 mm. |
| L318 | H12: line over last 8 setpoints in sequence time, "evaluates it 60 ms ahead of the newest packet"; 100 ms horizon; reject clauses | `strategies/deadreckon.py` (L 0.060, K 8, H 0.100); audit_strategies F1 | STALE | Post-fix the lead over the baseline playout is exactly L = 60 ms; the line is evaluated at `newest seq time + age − interp_s + L`, i.e. 30 ms ahead of the newest packet. The pre-fix code evaluated 60 ms ahead of the newest packet (90 ms lead), and the archived pre-fix Tier 1 D arm is labelled so by `aggregate.py --legacy-deadreckon-lead`. |
| L320 | H14 Gain: K·L = 1/e; scales operator speed not wire; reject clauses | `strategies/adaptive_gain.py`; selection.md §3, §4 | OK / STALE | Add L0: selection.md §4 hard-coded L0 = 0.29 s; post-fix (audit F5) L0 is computed as playout + half a telemetry period + tau_h + measured zero RTT = 30 + 16.7 + 170 + 2 = **0.219 s**. With 0.29 the scale stayed at 1.0 on `leo_relay`, so pre-fix G was identical to B there. |
| L322 | H11 Terminal: ≤8 cm, 2 s, ≤7 cm/s, crossfade; P vs Pg; reject clauses | `strategies/terminal.py` (REACH 0.08, MAX_S 2.0, V 0.07, BLEND_S 0.5 stretched to 4·gap/vmax); selection.md §3 | OK | — |
| L324 | H20 chained release, tether, Cr charged; reject clauses | `sim/__init__.py`; `ground/operators.py`; selection.md §3 | OK | See missing topic 6 (tether damping). |
| L328 | Common settings; eleven profiles | selection.md §3 | OK | Eleven for blocks A/B/E; block S adds two. |
| L330–335 | Tier 1 table A, B, C, E | selection.md §3; `matrix.py` TIER1 | STALE | Block S is missing: tasks `capture`, `peg` × arms B, T, D, G, P, Pg × `leo_relay_drop1`, `leo_relay_drop12`, tau_h 0.17 = 24 cells. |
| L337 | "Tier 1 is 174 cells × 30 seeds = 5,220 episodes, about 4 h" | results.md header (198 cells, 5,940 episodes); commit dd26840 | STALE | 198 cells, 5,940 episodes with block S. |
| L337 | Tier 2: 100 paired seeds on the knee cell plus each file's gating cell; McNemar power | selection.md §3; `matrix.py` TIER2 (commit bfab058) | STALE (incomplete) | Tier 2 cells were re-pointed from the post-fix knees (capture B 240 / T 471 ms; peg B 97 / D 256 / T 510 ms; G, P never): T = capture sweep:400, geo_relay, zero + peg sweep:400, sweep:500; D = capture sweep:250 + peg leo_relay, sweep:100, sweep:250; G = peg sweep:250, sweep:500, geo_relay, zero + capture sweep:400, zero; P = peg leo_relay, sweep:400 + capture sweep:500, leo_relay; Pg = peg leo_relay, sweep:400; C = capture_chain leo_relay, sweep:500. |
| L343–357 | Rejected/deferred table, one line each | selection.md §5 | OK | — |
| L438 | Muon Halo Q1 2027; SYNTHESIS §9 Q1 | leo_link_options §2 b1; SYNTHESIS §9 | OK | — |
| L439–440 | Kepler 2028; Telesat 2028; Amazon Leo NASA demo | leo_link_options §2 b2–b4 | OK | — |
| L441 | No rad-tolerant board with HW encode | hardware_landscape §5 | OK | — |
| L442 | Only 2–3 component GMM validated; `dist` has exp, gauss, lognormal, gamma | network_emulation §1.1; `link/emulator.py` `_UNIT` | OK | The lognormal shape is a fixed σ = 0.6 (`_S`); only the SD is a profile knob. |
| L443 | Ruby 1.3M USV 20 krad | hardware_landscape §4 (10 and 20 krad) | OK | — |
| L444 | RFC 7679; RTT/2 wrong by ≈8 ms | network_emulation §2 (52 vs 35 ms) | OK | — |
| L445 | 45 sites ≈64 % duty; ≈1× per operator-hour | scores_V1.md (64.5 %, 66 windows/day) | OK | — |
| L453–461 | Nine open questions | SYNTHESIS §9 | OK | — |
| L465–467 | Grasp physics; operator model; VISTA 97 → 35 % | SYNTHESIS §3; multi_operator_bandwidth §1.3 | OK | — |
| L468 | "One-way delay is exact only because both ends share a clock" | `ground/loop.py` docstring; audit T18 | WRONG | The testbed does not record true one-way delay: `owd_up_ms = rtt_ms / 2`, off by ≈6 ms on `leo_relay` (30/18 split). Exact OWD is only *possible* on one host; it is not logged. |
| L469–471 | No force; unique tasks absent; `ramp_clip` thousands per episode, decidable pair | teleop_fundamentals §4; `strategies/terminal.py` docstring | OK | — |
| L483 | "eight research reports + SYNTHESIS.md" | `docs/research/` (8 + SYNTHESIS) | OK | — |
| L485 | experiments/: results.md, results.json, paired.md, raw/ | `docs/experiments/` | STALE (incomplete) | Also `curves.json`, `audit_testbed.md`, `audit_strategies.md`, `tier1/`, `tier1_prefix/`, `raw_tier1_prefix/` (archived pre-fix), `raw_tier2/`. |
| L491 | profiles.py: zero, direct_gs, leo_relay, geo_relay, sweep | `link/profiles.py` | STALE | Add `leo_relay_drop1`, `leo_relay_drop12`. |
| L497 | strategies list | `strategies/__init__.py` | OK | Registry also has `deadreckon30` (L = 30 ms, Tier 2). |
| L503, L512 | "53 tests"; "about one minute (verified 2026-09-12)" | `uv run pytest -q --collect-only` on 2026-09-13 | STALE | 77 tests collected. Runtime not re-measured here. |
| L522–528 | Tier 1 commands (blocks A, B, C, E) | `matrix.py` TIER1 | STALE | Block S is not run by the listed commands; add `--blocks S --strategies baseline,twin,deadreckon,gain,terminal,terminal_ground`. |
| L529–530 | Tier 2 `--arm-map T=twin,D=deadreckon,G=gain,P=terminal` | `matrix.py` TIER2 | OK (incomplete) | TIER2 also keys `Pg=terminal_ground` and `C=baseline`; unmapped labels are dropped, so the listed command skips the Pg and C confirm cells. |
| L536 | Resume/retry semantics; aggregate outputs | `matrix.py`, `aggregate.py` docstrings | OK | `aggregate.py` also writes `curves.json` and supports `--exclude-stalled`. |

## 2. Missing topics the report should cover

1. **The audit and its fixes (Phase 5).** `docs/experiments/audit_testbed.md` (T01–T20) and `audit_strategies.md` (F1–F13) changed what the numbers mean and are not mentioned. At minimum: T01 physical-jaw capture at 25 mm; T02 block S; T03 random spike/outage phase; T04 arrival-stamped RTT (zero cell 10 → ≈1 ms); T05 smoothness on the sidecar (the `observation.state` path moved `stall_frac` 0.24 → 0.51 on a synthetic move); T06 stall flags and `--exclude-stalled`; T07 `cage` out of the gate; T08 `sweep:0` ≈12 ms realised; T09/F2 linger-free durations; T10 per-pass throughput; T11 v2.1 metadata; T17 reordering ≈6–8 %; T18 `owd_up_ms` = RTT/2; F1 DeadReckon lead = L; F5 Gain L0 = 0.219 s; F9 `vel_over` cannot fire for any arm by construction (the ramp limiter bounds Δq ≤ vmax·dt), so a zero there is a consistency check, not an independent safety measurement.
2. **Two Tier 1 result sets exist.** Pre-fix (commit be74e18, archived under `raw_tier1_prefix/`, `tier1_prefix/`) and post-fix (dd26840, `raw/`, `results.md`). §9 must say which one it reads and that the pre-fix D arm ran with a 90 ms lead.
3. **The zero-latency ceiling is ≈0.63, not 1.0.** Post-fix results.md: baseline `zero` 19/30, `sweep:0` 20/30, all arms 0.60–0.67 on `zero`. Audit T16 attributes the failures to 7 reachability seeds, not the envelope. Every "fraction of own zero" ratio in §9.2 is relative to this, and the report should say why the operator does not reach 100 % without latency.
4. **What the dropout test actually is.** PROGRAM.md's "zero unsafe motion on dropout" is carried only by block S (`leo_relay_drop1`/`drop12`); Poisson outages land inside a 20 s episode 0.9 % of the time per direction, so blocks A/B/E never exercised hold or retract (T02). Results.md already has a "Block S: forced dropout" section with hold/retract counts; §7.4, §7.5 and §9.2 need to point at it.
5. **Testbed-vs-architecture deviations, in one list.** No duplicate-send (single UDP send; H05's null is the justification); no acceleration clamp; no `link_state` column; `owd_up_ms` = RTT/2; `geo_relay` ±13 ms orbital drift (network_emulation §4 P3) not implemented; `direct_gs` loss is constant, the SYNTHESIS §7 "×10 below 10° elevation" is not implemented; lognormal shape fixed at σ = 0.6; video is padding bytes only; Gilbert-Elliott parameters are per packet at 50 Hz with no rate dependence.
6. **H20 tether is not force-free.** `sim/__init__.py` `_tendon` docstring: MuJoCo tendon damping has no dead band, so the slack thread applies −0.01·v while `taut_frac` reads 0; a box released at 4.1 cm/s is at 2.5 cm/s after 6 s (τ ≈ 5 s). The "free 6-DoF body" subset claim in §8.3/§9.5 needs this caveat.
7. **Stream bitrate.** Reconcile 1 Mbps (live, Scenario A, 10.4 Mbps total) with 1.5–2.5 Mbps (hardware_landscape §4 logged cameras) in §1, §4.1, §4.6 and §5.
8. **Tier 2 as run.** The re-pointed Tier 2 cells and knees (commit bfab058, `matrix.py` TIER2 comment) and the `deadreckon30` arm; §8.4 still describes Tier 2 as provisional.
9. **Contamination policy.** `stalls` / `max_dt_s`, the CONTAM_EPS = 3 flag in `aggregate.py`, and whether §9 excludes stalled seeds; audit T06 found 8 contaminated cells in the pre-fix run.
10. **Promised appendices.** selection.md §5 defers H01 to a 20-line `orbit.py` geometry rerun and adopts H04's outage-rate sweep (1.7, 5, 12 h⁻¹) as an appendix row. The report has neither; either include them (scores_V1 already has the 4-polar and 45-site numbers) or state they were not run.
11. **Multi-arm scaling (§9.6).** No matrix block runs `--arms 4`; say whether the scaling case was run at all and, if so, from which command.
12. **Block E rationale.** tau_h 0.25 exists because every hypothesis file quoted 0.25; the report lists block E without saying why it exists or that with the pre-fix L0 the Gain arm on block E was a different controller (F5).
13. **Verification section.** State the test count (77) and the date it was run, and that `vel_over = 0` is guaranteed by construction (F9) so the audit's independent safety evidence is `move_in_hold` and `keepout` on block S plus the sidecar velocity check.

REVIEW_DONE.
